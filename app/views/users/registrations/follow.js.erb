// remove user from "users to follow"
className = ".user-follow-link-<%= @other_user.id %>";
$(className).parentsUntil('.user-to-follow').parent().hide(500);

// load new "users to follow"
// TODO

// remove tweet cards of unfollowed user
className = ".tweet-follow-link-<%= @other_user.id %>";
$(className).parentsUntil('.col-tweet').parent().hide(500);
