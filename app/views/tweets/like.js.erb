id = "#like-icon-<%= @tweet.id %>"
elem = $(id).children();

<% if @tweet.liked?(current_user) %>
  elem.removeClass('far');
  elem.addClass('fas');
<% else %>
  elem.removeClass('fas');
  elem.addClass('far');
<% end %>
