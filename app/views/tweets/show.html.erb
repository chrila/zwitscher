<h1 class="display-3 my-3">Tweet</h1>

<%= render @tweet, retweet: false %>

<h2 class="display-4">Likes</h2>
<ul>
  <% @tweet.likes.each do |like| %>
    <li class="tweet-detail-li">
      <img class="user-avatar" src="<%= like.user.pic_url %>" title="<%= like.user %>" />
    </li>
  <% end %>
</ul>